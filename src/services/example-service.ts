export {};

// Example of a ReactService

/*
export const fetchLibrarySongs = (offset: number): Promise<any> => {
  return (window as any).MusicKit
  .getInstance().api.library.songs(null, { limit: 100, offset: offset });
}
 */
